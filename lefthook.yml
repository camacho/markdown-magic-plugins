pre-commit:
  parallel: true
  commands:
    format:
      glob: '*.{js,json,md,yml,yaml}'
      run: pnpm prettier --write {staged_files}
      stage_fixed: true
    test:
      glob: 'packages/*/**/*'
      run: pnpm -r --filter='...[HEAD]' test
